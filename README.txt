RU 📌 Установка Return Launchpad

1. Перетащите Return Launchpad.app в папку Applications.
2. При первом запуске система может показать предупреждение "Не удается открыть программу".
   
   Выберите один из способов:
   
   Способ 1 (Рекомендуемый):
   - Кликните правой кнопкой мыши на приложение в папке Applications
   - Выберите "Открыть" в контекстном меню
   - Подтвердите открытие в диалоге безопасности
   
   Способ 2 (Через Terminal):
   - Откройте Terminal и выполните команду:
     xattr -cr "/Applications/Return Launchpad.app"
   - Затем запустите приложение обычным способом

3. После первого успешного запуска приложение будет работать как обычно.

Приложение использует Ad Hoc подпись и работает на любом Mac без требования Apple ID.
✨ Universal Binary: Поддерживает как Intel (x86_64), так и Apple Silicon (arm64) процессоры.

---

EN 📌 Installing Return Launchpad

1. Drag the Return Launchpad.app to the Applications folder.
2. On first launch, the system may show a warning "Cannot open the program".
   
   Choose one of the methods:
   
   Method 1 (Recommended):
   - Right-click on the application in the Applications folder
   - Select "Open" from the context menu
   - Confirm opening in the security dialog
   
   Method 2 (Via Terminal):
   - Open Terminal and run the command:
     xattr -cr "/Applications/Return Launchpad.app"
   - Then launch the application normally

3. After the first successful launch, the application will work as usual.

The application uses Ad Hoc signing and works on any Mac without requiring an Apple ID.
✨ Universal Binary: Supports both Intel (x86_64) and Apple Silicon (arm64) processors.